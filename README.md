# インタプリタ

ソースコード -> トークン列 -> 抽象構文木

## 字句解析

ソースコードをトークン列に変換する。

- トークン: 言語の最小単位 (キーワード、識別子、リテラル、演算子など)
- 字句解析器 (Lexer): ソースコードを読み込み、トークン列を生成するプログラム

## 構文解析

トークン列を抽象構文木 (AST) に変換する。

- 抽象構文木 (AST): プログラムの構造を表す木構造
- 構文解析器 (Parser): トークン列を読み込み、ASTを生成するプログラム

## REPL

Read-Eval-Print Loop の略。対話型のプログラム実行環境。

- Read: ユーザーからの入力を読み込む
- Eval: 入力を評価 (実行) する
- Print: 評価結果を表示する
- Loop: これらのステップを繰り返す